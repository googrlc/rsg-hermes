# Hermes Tool Map

Operator reference for what the conversational agent can run today. The **Live
runtime tools** table below is generated from the same source as the Command
Center skills API (`GET /api/command-center/skills`) — regenerate it whenever
tools are added or removed:

```bash
source .venv/bin/activate
python scripts/gen_tool_map.py
```

Use `--check` in CI to fail when the doc drifts from the live catalog.

The **Source** column is defined in `scripts/gen_tool_map.py` (`TOOL_SOURCES`) and
must match the handlers in `hermes/agent/nl_agent.py`. Zoho ↔ Supabase sync
cadence and conflict rules: `docs/zoho-supabase-sync-design.md`.

## Live runtime tools

<!-- LIVE_RUNTIME_TOOLS_BEGIN -->
_Regenerated by `scripts/gen_tool_map.py` from `GET /api/command-center/skills` — 21 tools. Do not edit by hand._

| Tool | Source | Description |
| --- | --- | --- |
| `ams_client_snapshot` | NowCerts (live AMS) | Live AMS (NowCerts) snapshot for one client, pulled straight from NowCerts (not the nightly mirror): the insured's current identity/status, in-force policies (c |
| `appointments_by_line` | Supabase (`carriers`) | The panel grouped by line of business — which carriers RSG can place each line with, and whether the appointment is direct or through a general agent |
| `case_progress` | Supabase (`agency_crm_*`, legacy → Zoho) | One case in full with its task checklist — who owns each task, what's due, and which required items are outstanding |
| `client_documents` | Nextcloud | A client's documents in Nextcloud (COIs, policies, proposals, quotes, correspondence, renewal reviews) |
| `client_policies` | Canonical book (Supabase, NowCerts-sourced) | Show a client's book — their policies (with active count) from the canonical book |
| `commission_shortfalls` | Supabase (`commission_ledger`) | List the specific policies where RSG was underpaid or is missing a carrier statement — ranked by dollars outstanding |
| `commission_summary` | Supabase (`commission_ledger`) | Summarize RSG's commissions from the reconciled ledger — total expected vs received and what's still outstanding, optionally for one carrier |
| `crm_client_activity` | Supabase (`agency_crm_*`, legacy → Zoho) | Open cases and their tasks for a client — read from the legacy Supabase agency CRM tables (`agency_crm_cases` / `agency_crm_tasks`) while service cases migrate |
| `email_search` | Microsoft 365 Graph | Search the connected agency mailbox (Microsoft 365) for recent emails |
| `find_client` | Canonical book (Supabase, NowCerts-sourced) | Search the canonical client book (NowCerts insureds) by name |
| `intake_lead` | Supabase intake; Zoho CRM when `HERMES_WRITE_TO_ZOHO` | Process a casual lead intake message |
| `investigate_policy` | NowCerts + Supabase mirror + renewals (read-only) | Cross-system data-quality investigation for ONE policy number |
| `list_carriers` | Supabase (`carriers`) | List the carriers RSG has appointments/data on, optionally filtered by name or line of business |
| `list_cases` | Supabase (`agency_crm_*`, legacy → Zoho) | Open service cases with their checklist progress — task counts, what's blocking, and what's ready to close |
| `list_intake_submissions` | Supabase (`intake_submissions`) | List recent intake submissions and their status (awaiting_approval, failed, completed) |
| `list_skills` | Hermes catalog (meta) | List Hermes's own capabilities — the live tools it can run plus the domain playbooks available |
| `lookup_class_code` | Supabase (`gl_class_codes`, `wc_class_codes`) | The class-code reference (gl_class_codes / wc_class_codes) — what a code MEANS, not who writes it |
| `match_carrier_appetite` | Supabase (`carrier_appetite`) | Find carriers whose appetite matches a risk — by line of business, state, and/or class/NAICS |
| `renewals_overview` | Supabase (`project_85_renewals`) | Upcoming policy renewals and at-risk/retention clients from the Project 85 renewal watchlist (classified risk + premium) |
| `run_report` | Supabase + Hermes report dispatcher | Run a CRM report or dashboard view |
| `web_research` | Public web | Research a business/client on the public web — website, owners, NAICS/SIC, phone, address, insurance context |
<!-- LIVE_RUNTIME_TOOLS_END -->

## Domain playbooks

Domain skills (`.claude/skills/*/SKILL.md`) are listed by the same API under
`domain_skills` but are not auto-synced into this doc yet. Call
`GET /api/command-center/skills` or use the `list_skills` agent tool.
