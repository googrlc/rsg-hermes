"""Hermes document library — save/list docs across Supermemory + index.

Client PDFs are not stored here. They are filed through
``hermes.documents.registry`` into Nextcloud ``Clients/{name}/`` with a
Zoho ``Document_Registry`` metadata row.
"""
