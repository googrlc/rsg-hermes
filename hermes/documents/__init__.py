"""Hermes document library — save/list docs across Supermemory + index.

The Zoho Document_Registry / Nextcloud Team Folder pipeline lives in
``hermes.documents.registry`` (files in Nextcloud, metadata in Zoho CRM).
This package's ``store`` is the searchable index of documents Hermes authors,
not the agency file store.
"""
